{
  "name": "shopint-parse-multi-url",
  "version": "1.0.0",
  "scripts": {
    "local": "sam local start-lambda -p 9001 --region eu-central-1 --profile shopint",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "sam local invoke --profile shopint --region eu-central-1 -e event.json parseMultiUrl",
    "package": "sam package --template-file template.yaml --profile shopint --s3-bucket shopint-functions --output-template-file packaged.yaml",
    "deploy": "aws cloudformation deploy --profile shopint --region eu-central-1 --template-file packaged.yaml --stack-name parseMultiUrl --capabilities CAPABILITY_IAM",
    "publish": "npm run package && npm run deploy"
  },
  "dependencies": {
    "amazon-buddy": "file:amazon-buddy",
    "bluebird": "^3.7.2",
    "chrome-aws-lambda": "2.0.x",
    "jsdom": "^16.4.0",
    "jsonld-parser": "^0.6.1",
    "node-os-utils": "^1.3.2",
    "puppeteer-core": "2.0.x",
    "url-parse": "^1.4.7"
  }
}
