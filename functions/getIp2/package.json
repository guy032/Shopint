{
  "name": "shopint-get-ip2",
  "version": "1.0.0",
  "scripts": {
    "local": "sam local start-lambda -p 9003 --region eu-central-1 --profile shopint",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "sam local invoke --profile shopint --region eu-central-1 -e event.json getIp2",
    "package": "sam package --template-file template.yaml --profile shopint --s3-bucket shopint-functions --output-template-file packaged.yaml",
    "deploy": "aws cloudformation deploy --profile shopint --region eu-central-1 --template-file packaged.yaml --stack-name getIp2 --capabilities CAPABILITY_IAM",
    "publish": "npm run package && npm run deploy"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "google-search-results-nodejs": "^2.0.1",
    "http": "^0.0.1-security"
  }
}
